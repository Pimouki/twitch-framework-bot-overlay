export declare module ObjectUtils {
    const updateItem: <T, U extends keyof T>(current: T, key: U, value: T[U]) => T;
    const removeItemById: <T, U extends keyof T>(current: T, key: U) => Omit<T, U>;
}
