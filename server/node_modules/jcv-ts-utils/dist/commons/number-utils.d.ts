export declare module NumberUtils {
    function lerp(t: number, a: number, b: number): number;
    const PI: number;
    const PI2: number;
    const HALF_PI: number;
    const QUART_PI: number;
    const TENTH_PI: number;
    const TWENTIETH_PI: number;
    const rangeLoop: (min: number, n: number, max: number) => number;
    const angleRangeLoop: (angle: number) => number;
    const clamp: (min: number, n: number, max: number) => number;
    const round: (n: number, precision: number) => number;
    const tens: (n: number) => number;
    const finales: (n: number) => number;
    function inRange(n: number, min: number, max: number): boolean;
    /**
     *
     * @param point nombre de point qui seront converties en valeur
     * @param scalePoint échelle de grandeur de points à avoir pour atteindre le cap maximum
     * @param maxVal
     * @param minVal minimum value
     */
    const scaleHyperTangent: (point: number, scalePoint: number, maxVal: number, minVal?: number) => number;
    const randomRange: (max: number, min?: number) => number;
    const average: (numbers: number[]) => number;
}
