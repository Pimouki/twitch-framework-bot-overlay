"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const a_star_1 = require("./a-star");
describe("SearchAStar", () => {
    const grid = [
        { x: 0, y: 0 },
        { x: 1, y: 0 },
        { x: 2, y: 0 },
        { x: 0, y: 1 },
        { x: 1, y: 1, weight: 0 },
        { x: 2, y: 1 },
        { x: 0, y: 2 },
        { x: 1, y: 2 },
        { x: 2, y: 2 },
    ];
    it("finds the shortest path", () => {
        const start = { x: 0, y: 0 };
        const end = { x: 2, y: 2 };
        const expectedPath = [
            {
                x: 1,
                y: 1,
            },
            {
                x: 2,
                y: 2,
            },
        ];
        const result = (0, a_star_1.searchAStar)(grid, start, end, {
            closest: false,
            diagonal: true,
        });
        expect(result).toEqual(expectedPath);
    });
    it("finds the shortest path vertical only", () => {
        const start = { x: 0, y: 0 };
        const end = { x: 0, y: 2 };
        const expectedPath = [
            { x: 0, y: 1 },
            { x: 0, y: 2 },
        ];
        const result = (0, a_star_1.searchAStar)(grid, start, end, {
            closest: false,
            diagonal: false,
        });
        expect(result).toEqual(expectedPath);
    });
});
//# sourceMappingURL=a-star.test.js.map