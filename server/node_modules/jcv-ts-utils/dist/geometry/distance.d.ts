import { Point } from "./point";
import { Circle } from "./circle";
export declare module Distance {
    import Point2 = Point.Point2;
    import Circle2 = Circle.Circle2;
    const inRange: <T extends Point2>(which: Point2, others: T[], compareFunction: (distance: number) => boolean) => T[];
    function nearest<T extends Point2>(which: Point2, others: T[]): {
        distance: number;
        item: T | undefined;
    };
    function further<T extends Point2>(which: Point2, others: T[]): {
        distance: number;
        item: T | undefined;
    };
    function pointToCircle(point: Point2, { x, y, radius }: Circle2): number;
    function circleToCircle({ x: x1, y: y1, radius: r1 }: Circle2, { x: x2, y: y2, radius: r2 }: Circle2): number;
    function pointToPoint({ x: p1x, y: p1y }: Point2, { x: p2x, y: p2y }?: Point2): number;
}
