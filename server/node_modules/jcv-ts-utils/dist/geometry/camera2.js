"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Camera2 = void 0;
class Camera2 {
    x = 0;
    y = 0;
    scaleX = 1;
    scaleY = 1;
    apply(ctx) {
        this.applyScale(ctx);
        this.applyTranslation(ctx);
    }
    screenToWorld({ x: px, y: py }) {
        return {
            x: px / this.scaleX + this.x,
            y: py / this.scaleY + this.y,
        };
    }
    worldToScreen({ x: px, y: py }) {
        return {
            x: (px - this.x) * this.scaleX,
            y: (py - this.y) * this.scaleY,
        };
    }
    applyScale(ctx) {
        ctx.scale(this.scaleX, this.scaleY);
    }
    applyTranslation(ctx) {
        ctx.translate(-this.x, -this.y);
    }
}
exports.Camera2 = Camera2;
//# sourceMappingURL=camera2.js.map