import { Point } from "./point";
export declare module Triangle {
    type Triangle2 = {
        p1: Point.Point2;
        p2: Point.Point2;
        p3: Point.Point2;
    };
    /**
     *
     * @param base length of triangle base
     * @param a length of a side
     * @param b length of another side
     * @return length of H
     */
    const calculH: (base: number, a: number, b: number) => number;
}
